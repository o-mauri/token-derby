// Canonical grid lives in @token-derby/shared. Re-exported here so the sprite
// renderers ported from site/src keep their existing relative imports intact.
export { GRID, SPRITE_WIDTH, SPRITE_HEIGHT, HOOF_COLOR, type SlotTag } from '@token-derby/shared';
