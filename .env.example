# Admin username used to log in before announcing a release.
ADMIN_USERNAME=

# Admin password used to log in before announcing a release.
ADMIN_PASSWORD=

# Base URL of the API the announce script posts to. Optional — defaults to
# https://token-derby.mauricode.co.uk/api if unset.
TOKEN_DERBY_API_BASE=
