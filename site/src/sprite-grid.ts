// Canonical grid now lives in @token-derby/shared. Re-exported here so existing
// site imports (./sprite-grid.js) keep working unchanged.
export { GRID, SPRITE_WIDTH, SPRITE_HEIGHT, HOOF_COLOR, type SlotTag } from '@token-derby/shared';
